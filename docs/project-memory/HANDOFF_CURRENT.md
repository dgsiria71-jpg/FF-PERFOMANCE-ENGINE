# Current Handoff — 2026-09-25

## Exact repository and application checkpoint

- Repo `dgsiria71-jpg/FF-PERFOMANCE-ENGINE`, dev branch `build/initial-product`, draft PR #1, `main` untouched. Product DG Performance Engine; preserve `FFPerformanceEngine.*`, original FF/BlueStacks specialization and tested Tracks0–5. Work via ChatGPT+GitHub, not an invented Codex workspace.
- **Latest application SHA `8187d93bf785ee786a7dbb5f1ec83c9d694962e2`**, `feat: enforce OS-owned Guardian session continuity through canary execution`.
- Exact **Windows CI #1079 SUCCESS**, run `36149662557`, job `108119431062`: native configure/build/test, .NET build, Core and App full self-tests, win-x64 publish, upload and cleanup all SUCCESS.
- Artifact `FFPerformanceEngine-win-x64`, ID `10870847400`, SHA-256 `6f6a636614500e1e776e5eb5d89e61715c033425a8e9ebab7ec53b414ce7c8a1`.
- Previous verified app `d38ce821026c214719d3b7d49821b242041c908e`, Windows CI #1077 run `35467919294` SUCCESS. Previous documentary SHA `658453d957d01cc2519889c1175ed8ebf6699146`, Windows CI #1078 run `35468210463` SUCCESS. THIS documentary update requires its own fresh exact-commit Windows CI; do not claim documentary GREEN until confirmed.
- Mandatory startup read order: `AGENTS.md` → `docs/project-memory/README.md` → this handoff → `IMPLEMENTATION_STATUS.md` → `CANONICAL_CONTEXT.md` → canonical `docs/superpowers/specs/2026-09-06-dg-performance-engine-unified-architecture-design.md` → affected code/tests. Actual branch/code/exact CI outrank old memory. Expanded master `RECOVERED_MASTER_ARCHITECTURE_2026-09-11.md`; never invent original absent Track11–19 names.

## Verified Track state

Tracks0–5 GREEN only for implemented canonical scopes. Track6 Adaptive Guardian2.0 ACTIVE: item1 workload state and item2 evidence-backed supported classifiers GREEN; **item3 session optimizer IN PROGRESS**; item4 learned reliability and item5 post-session queue PENDING. Tracks7–10 PLANNED. Preserve `Observed != Validated`, stable GameId != ephemeral PID, actual LiveSafe capability, exact Track2 rollback, Track0 global benchmark authority, Track4 typed telemetry and strict Track5 profile provenance. No startup discovery and NO automatically enabled generic Guardian.

Item3 earlier checkpoints already completed: Active/High exact LiveSafe eligibility CI #1051; reversible canary transaction/lease #1053; typed CPU/GPU evaluator #1058; actual Windows LiveSafe capability gate #1060; atomic per-session budget/cooldown #1062; exact action→mutation catalog #1064; structural comparability #1066; mandatory executor comparison with test-only source #1068 (doc #1069); Track0 process-local generation #1070; bounded physical interval observer #1071; entire canary before→mutation→after real benchmark generation gate #1073 (doc #1074). Full hashes, RED/GREEN tests and scope in IMPLEMENTATION_STATUS, ROADMAP and historical checkpoints.

### Standalone Windows physical process proof — application CI #1075 and docs #1076

`GenericGuardianWindowsProcessLifetimeProbe.cs` reads actual Windows PID, fully qualified `MainModule.FileName`, OS process creation `StartTime` UTC, rechecks PID/path/creation time across observations. Missing/denied/exited/reused process returns unavailable. RED verifier `1a4bc40ae303c7397647f63aff09afcc5c64fb67` / run `35465631677` had expected single CS0246, zero compiler warnings; GREEN verifier `e966dee5d881bd2efa740fa10836cc700c5ebca8` / `35465714691` all steps SUCCESS. Official `d06eda...` CI #1075 SUCCESS and documentary `f4fb0d...` CI #1076 SUCCESS. Checkpoint `checkpoints/2026-09-19-track6-process-lifetime-scene-source-audit.complete`. Proof is OS process continuity only, not game scene or source attestation.

### NEW OS-owned epoch coordinator — application Windows CI #1077

Isolated verifier `ci/track6-physical-session-owner-verify`: RED `ca2424757cecab3442a5c517465a3e3d942b929a`, run `35467577760`, job `105962631690`: native PASS, exactly one expected missing-type `GenericGuardianWindowsSessionLifecycleCoordinator` CS0246, zero compiler warnings. First implementation `ec7da76302baf3d7f6c107c1acbe254db457fef4`, run `35467675110`: native PASS, one CS8602 nullability error inside new TEST, not production. Fixed test `1d1ffd687920ca497c786145423635b6a694ed32`, verifier run `35467771749`, job `105963141142` full SUCCESS native/managed/Core/App/publish. Official selective `d38ce821026c214719d3b7d49821b242041c908e` Windows CI #1077 full SUCCESS, exact artifact above. Official diff exactly three files: new `GenericGuardianWindowsSessionLifecycleCoordinator.cs`, new real-Windows `GenericGuardianWindowsSessionLifecycleSelfTests.cs`, one test registration in `GenericGuardianWindowsProcessLifetimeSelfTests.cs`. Ephemeral workflow excluded. Checkpoint `checkpoints/2026-09-19-track6-os-owned-session-epoch.complete`.

Coordinator uses concrete OS probe, not caller flags or Guid. For Active/High exact capturable Track3 workload only, reads real PID/path/creation time and issues its own Guid session epoch. It returns reference-identical key for an unchanged physical lifetime; any invalid state, inaccessible process, changed path/PID/creation time retires old key. `IsCurrent` requires exact owned key object + exact GameId/PID/path and actual OS recheck; cloned/forged keys denied; Reset retires epoch. Test uses actual Windows self-test runner, NOT synthetic PID 4242 as OS authority. Coordinator does not restore kept leases: the owner host MUST restore prior to reset/rebind.

### NEW executor-bound OS-owned session continuity — application Windows CI #1079

Isolated TDD RED on `ci/track6-session-owner-executor-verify`: commit `1d6d1ed8ca4607cd49ed8e4d77bb69a4c69b3ece`, run `36148963987`, job `108117098536`, native PASS then exactly one expected `CS1739` because `GenericGuardianWindowsSessionCanaryExecutor` had no `sessionOwner` parameter, zero compiler warnings. GREEN final isolated branch `ci/track6-session-owner-executor-final-verify`: commit `d58b615b22ad6027223f3129ac03ef0a22d444ae`, run `36149411454`, job `108118596564`, all native/managed/Core/App/publish/upload steps SUCCESS. Official selective app `8187d93bf785ee786a7dbb5f1ec83c9d694962e2`, Windows CI #1079 full SUCCESS, exact artifact above. Official diff is exactly five code/test files; both temporary verifier workflows excluded.

Executor preflight now requires the exact reference-identical key issued by `GenericGuardianWindowsSessionLifecycleCoordinator` and a fresh `IsCurrent` OS recheck. The same physical session is rechecked after the before capture, again immediately before mutation, after mutation, after the after capture, after comparison evidence, and after evaluator execution immediately before `KEEP`. Losing ownership before mutation returns Inconclusive/not attempted with no Windows transaction. Losing it after mutation restores through the existing Track2 session transaction, returns Inconclusive/rolled back and no active lease. Existing canary and benchmark-cycle positive tests now bind to the actual Windows self-test process instead of treating PID 4242 as physical authority. A dedicated regression resets the real owner at each boundary to prove deny-vs-rollback behavior.

## CRITICAL boundaries — do not misrepresent

OS process lifetime continuity is now enforced end-to-end inside the current canary executor path, but it still proves only **which Windows process lifetime** is being measured. It does NOT prove gameplay scene, mode, load or environment. No production `IGenericGuardianCanaryEvidenceSource` exists; test evidence strings/flags remain TEST ONLY. Generic adapter has no scene capability; BlueStacks/FF ADB foreground detects Android package, NOT in-game scene/mode/load. PresentMon/TelemetryFrame provide performance metrics, not authenticated gameplay context. Track0 generation is still observation, not mutual exclusion, and external benchmarks/other tools remain outside that detector. The final process check can also be followed by process exit before a future host notices; retained lease cleanup on session end remains a host responsibility. No generic host, real-game HIL, live FPS gain, production action registrations, profile promotion, learning or automatic Guardian activation. Specialized FF/BlueStacks intact; `main` untouched.

## EXACT continuation

1. Audit the existing specialized Free Fire/BlueStacks adapter and permitted owned signals for a **real production** `IGenericGuardianCanaryEvidenceSource` that can attest scene/mode/load/environment across BOTH complete measurement windows. Do not infer scene from foreground package, PresentMon, input or render activity. If the current product has no legitimate signal, record the unsupported gap and keep generic canary mutation disabled rather than fabricating IDs.
2. Investigate external benchmark and other-mutation evidence with explicit source coverage and blind spots. Track0 generation covers only in-process controlled benchmarks; design true host coordination/exclusion before claiming uncontaminated live experiments.
3. Owner-managed generic host: own exact OS epoch, workload state/classifier/eligibility, trusted action catalog, atomic budget/cooldown, reversible executor and ALL retained lease cleanup at end/failure; restore kept leases before coordinator Reset/rebind; coordinate Track0 benchmark exclusion plus specialized Guardian suspend/reconcile. Validate Windows and real-game HIL BEFORE any opt-in/automatic activation. Then Track6 item4 reliability and item5 post-session queue.

Cycle: inspect → TDD RED Windows isolated → minimal GREEN/full verifier → selective official code commit/exact CI → docs/checkpoint → documentary exact CI. Do not touch `main`.
